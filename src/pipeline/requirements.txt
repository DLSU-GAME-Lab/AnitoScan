# src/pipeline/requirements.txt

# Utility for Model Setup
# huggingface_hub==1.12.0

# Phase 1 Dependencies
# downgraded OpenCV to the last version that supports NumPy 1.x
opencv-python==4.10.0.84  # switch to opencv-python-headless in production

# Phase 2 Dependencies
numpy>=1.26.4,<2.0.0  # downgraded for NeuS2 support
torch>=2.11.0, <3.0.0
ultralytics>=8.4.46, <9.0.0

# Phase 3 Dependencies (MASt3R and DUSt3R)
scikit-learn >= 1.8.0
# torch
torchvision >= 0.26.0
roma >= 1.5.6
gradio >= 6.14.0
matplotlib >= 3.10.9
tqdm >= 4.67.3
# opencv-python
scipy >= 1.17.1
einops >= 0.8.2
trimesh >= 4.12.2
tensorboard >= 2.20.0
pyglet >= 1.5.31, <2
huggingface-hub[torch] >= 1.13.0
numpy-quaternion >= 2024.0.13
pycolmap >= 4.0.4
poselib >= 2.0.5

# Phase 4 Dependencies (NeuS2)
commentjson~=0.9.0
imageio~=2.16.0
# numpy~=1.21.2
pybind11~=2.7.1
# scipy~=1.7.1
# tqdm~=4.62.2
# opencv-python~=4.5.5.62
# trimesh
# tensorboard
